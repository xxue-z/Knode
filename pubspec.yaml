name: knode_workspace
publish_to: 'none'

environment:
  sdk: '>=3.8.0 <4.0.0'

workspace:
  - apps/knode_app
  - packages/core
  - packages/wiki
  - packages/chat
  - packages/quiz
  - packages/micro_server

dev_dependencies:
  monolith_runner: ^1.0.2
dependency_overrides:
  archive: ^4.0.7

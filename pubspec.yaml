name: knode
description: 知维 - 个人知识管理 Flutter 应用
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  sqflite: ^2.3.0
  path: ^1.8.3
  doc_text_extractor: ^2.1.0
  file_picker: ^8.0.0
  pdf: ^3.10.8
  archive: ^4.0.7
  share_plus: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

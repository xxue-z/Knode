name: micro_server
description: 知维微服务包 - 内嵌 HTTP 服务、WebDAV
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  core:
    path: ../core
  shelf: ^1.4.0
  shelf_router: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

name: core
description: 知维核心包 - 数据模型、数据库、AI 抽象层
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  path: ^1.8.3
  sqflite: ^2.3.0
  crypto: ^3.0.0
  dio: ^5.4.0
  flutter_secure_storage: ^9.0.0
  path_provider: ^2.1.0
  flutter_tts: ^4.0.0
  speech_to_text: ^6.6.0
  intl: ^0.19.0
  flutter_riverpod: ^2.5.1
  share_plus: ^9.0.0
  logger: ^2.0.0
  device_info_plus: ^10.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

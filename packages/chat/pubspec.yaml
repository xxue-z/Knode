name: chat
description: 知维 Chat 包 - AI 对话、RAG 检索
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  core:
    path: ../core
  dio: ^5.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

name: chat
description: 知维 Chat 模块 - AI 对话、RAG 检索
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  wiki:
    path: ../wiki
  flutter_riverpod: ^3.3.1
  flutter_markdown_plus: ^1.0.7
  dio: ^5.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  generate: true
